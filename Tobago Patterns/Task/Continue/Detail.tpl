            if (finishedtask == $Attribute.Model$Task.Define$Attribute.Type.Pascal$)
            {
                if (returns == TaskResult.Ok)
                {
                    var $Attribute.Type.Camel$ = ($Attribute.Type.Pascal$) p[0];
					$Attribute.Name.Plural.Pascal$.AddItem($Attribute.Type.Camel$);
                }
            }
