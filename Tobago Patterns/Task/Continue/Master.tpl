		    if (finishedtask == $Model.Name.Pascal$Task.Select$Attribute.Type.Pascal$)
		    {
		        if (returns == TaskResult.Ok)
		        {
					$Attribute.Type.Pascal$ = ($Attribute.Type.Pascal$) p[0];
		        }
		        else
		        {
					Cancel();
					return;
				}
            }
